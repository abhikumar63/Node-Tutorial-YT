const sayHi = (name) => {
    console.log(`Hello there ${name}`);
};


// Here we are sending only one thing so we can send it without
// creating an object and send the function directly.
module.exports = sayHi;